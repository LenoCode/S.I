package socket_installer.SI_behavior.interfaces.context;

import socket_installer.SI_context.external_context.ExternalContext;

public interface ExternalContextInitializator {
    void initializeExternalContext(ExternalContext externalContext);
}
